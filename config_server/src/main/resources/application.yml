spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://git@github.com:zhengaobinlonglive/zhengaobin-springcloud-demo.git
          search-paths:
          - configfiles
      label: dev
server:
  port: 8888
